// Java Eksempel - Metode i Java
/*public class Person {
    private String navn;
    private int alder;

    // Constructor
    public Person(String navn, int alder) {
        this.navn = navn;
        this.alder = alder;
    }

    // Metode til at få navnet på personen
    public String getNavn() {
        return navn;
    }

    // Metode til at få alderen på personen
    public int getAlder() {
        return alder;
    }

    // Metode til at ændre navnet på personen
    public void setNavn(String navn) {
        this.navn = navn;
    }

    // Metode til at øge alderen på personen med en given værdi
    public void fødselsdag(int år) {
        this.alder += år;
    }
}

// Opretter en ny person og bruger metoderne
Person person = new Person("Anna", 30);
System.out.println(person.getNavn());  // Output: Anna
System.out.println(person.getAlder()); // Output: 30

person.setNavn("Maria");
System.out.println(person.getNavn());  // Output: Maria

person.fødselsdag(1);
System.out.println(person.getAlder()); // Output: 31


//___----------------------------------------------------------

// JavaScript Eksempel - Metode i JavaScript
function Person(navn, alder) {
    this.navn = navn;
    
    this.alder = alder;

}
class  Car { }
// Metode til at få navnet på personen
Person.prototype.getNavn = function() {
    return this.navn;
};

// Metode til at få alderen på personen
Person.prototype.getAlder = function() {
    return this.alder;
};

// Metode til at ændre navnet på personen
Person.prototype.setNavn = function(navn) {
    this.navn = navn;
};

// Metode til at øge alderen på personen med en given værdi
Person.prototype.fødselsdag = function(år) {
    this.alder += år;
};

// Opretter en ny person og bruger metoderne
let person = new Person("Anna", 30);
console.log(person.getNavn());  // Output: Anna
console.log(person.getAlder()); // Output: 30

person.setNavn("Maria");
console.log(person.getNavn());  // Output: Maria

person.fødselsdag(1);
console.log(person.getAlder()); // Output: 31
*/
